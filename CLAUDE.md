# CLAUDE.md - 직장내 괴롭힘 판단기 Proto

## 프로젝트 개요
- **한 줄 요약**: 직장 상황 입력 → 근로기준법 제76조의2 기준 분석 → 괴롭힘 판단 + 대응 가이드
- **핵심 가치**: "이게 괴롭힘인지 모르겠다"는 직장인에게 법적 기준으로 즉시 판단 제공
- **타겟 유저**: 직장내 괴롭힘 의심 피해자, 인사담당자

## ⚠️ Proto 규칙 (필수!)
- ❌ **로그인/Auth 없음** — 모든 기능 비로그인 접근
- ❌ **실제 AI API 호출 없음** — Mock 응답 사용
- ❌ **Supabase/외부 DB 없음** — 로컬 JSON 파일
- ❌ **결제(토스페이먼츠) 없음**
- ✅ **로컬 JSON**으로 샘플 분석 결과 데이터
- ✅ **UI/UX 완성도**에 집중

## 기술 스택
```
Next.js 15 (App Router) + TypeScript + Tailwind CSS + shadcn/ui
```

## 핵심 기능 (근로기준법 제76조의2 3요건)

### 3요건 분석
1. **지위·관계 우위 이용**: 직급, 연차, 인원수, 고용형태 등
2. **업무 적정범위 초과**: 업무 지시의 합리성, 사적 용무, 인격 침해 등
3. **신체적·정신적 고통 또는 근무환경 악화**

### 판단 결과
- `likely_bullying` — 괴롭힘 가능성 높음
- `unlikely_bullying` — 괴롭힘 가능성 낮음
- `insufficient_info` — 정보 부족

## 페이지 구조

### 1. 홈페이지 `/`
- 서비스 소개
- CTA: "지금 무료 분석받기"
- 면책 배너 (하단)

### 2. 분석 페이지 `/analyze`
- 상황 입력 텍스트박스 (placeholder: 예시 상황)
- 분석 버튼
- 로딩 애니메이션 (2초 딜레이 후 mock 결과)

### 3. 결과 페이지 `/result/[id]`
- 3요건 카드 (각각 충족/미충족/불명확 표시)
- 종합 판단 배너 (괴롭힘 가능성 높음/낮음/불명확)
- 신뢰도 % 표시
- 대응 가이드:
  - 즉시 할 수 있는 행동
  - 증거 수집 방법
  - 신고 채널 (내부 고충처리, 노동청 등)
- 면책 배너

### 4. FAQ 페이지 `/faq`
- 자주 묻는 질문 아코디언

## 데이터 구조

### `/data/mock-results.json`
```json
[
  {
    "id": "result-1",
    "input": "팀장이 매일 퇴근 시간에...",
    "analysis": {
      "requirement1": { "met": true, "reason": "직급 상 우위 관계..." },
      "requirement2": { "met": true, "reason": "업무 지시 합리성 부족..." },
      "requirement3": { "met": true, "reason": "정신적 고통 호소..." }
    },
    "overallJudgment": "likely_bullying",
    "confidence": 85,
    "summary": "3요건 모두 충족, 직장내 괴롭힘에 해당할 가능성이 높습니다.",
    "responseGuide": {
      "immediate": "녹음, 메신저 캡처 등 증거 확보를 시작하세요",
      "evidenceCollection": ["업무 지시 메신저 캡처", "목격자 확보", "진료 기록"],
      "reportOptions": [
        { "channel": "내부 고충처리", "pros": "신속한 해결 가능", "cons": "2차 피해 우려" },
        { "channel": "노동청 신고", "pros": "법적 강제력", "cons": "처리 시간 길어짐" }
      ]
    }
  }
]
```

### `/data/faq.json`
```json
[
  {
    "question": "직장내 괴롭힘의 법적 정의는?",
    "answer": "근로기준법 제76조의2에 따르면..."
  }
]
```

## 필수 컴포넌트

### 1. DisclaimerBanner
```tsx
// 면책 배너 - 모든 페이지 하단
- ⚖️ AI 분석이에요, 법적 효력은 없어요
- 👩‍⚖️ 정확한 판단은 전문가에게!
- 🛡️ 개인정보는 저장하지 않습니다
```

### 2. CrisisBanner
```tsx
// 위기상담 배너 - 결과 페이지
- 자살예방상담전화 1393
- 정신건강위기상담전화 1577-0199
```

### 3. RequirementCard
```tsx
// 각 요건별 분석 결과 카드
- 요건 번호 + 이름
- 충족/미충족/불명확 배지
- 상세 설명
```

## UI 디자인 가이드

### 색상
- Primary: 파랑 계열 (신뢰감)
- Danger: 빨강 (괴롭힘 가능성 높음)
- Warning: 노랑 (정보 부족)
- Success: 초록 (괴롭힘 아님)

### 톤앤매너
- 따뜻하고 공감하는 어조
- "~입니다" 대신 "~이에요"
- 피해자 입장 존중

## 구현 순서

### Phase 1: 프로젝트 셋업
```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"
npx shadcn@latest init
npx shadcn@latest add button card input textarea badge accordion
```

### Phase 2: 데이터 + 레이아웃
- `/data/mock-results.json` 작성 (3개 샘플)
- `/data/faq.json` 작성 (5개)
- 공통 레이아웃 (Header, Footer, DisclaimerBanner)

### Phase 3: 핵심 페이지
- 홈페이지 (`/`)
- 분석 페이지 (`/analyze`)
- 결과 페이지 (`/result/[id]`)
- FAQ 페이지 (`/faq`)

### Phase 4: 완성도
- 반응형 디자인
- 로딩 애니메이션
- CrisisBanner
- 메타 태그 (SEO)

## 완료 조건
- [ ] `npm run build` 성공
- [ ] `npm run dev`로 모든 페이지 접근 가능
- [ ] 분석 → 결과 플로우 동작
- [ ] 모바일 반응형
- [ ] 면책/위기상담 배너 표시

---
**PRD 참조**: https://idea-hub-theta.vercel.app/workplace-bully/docs


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>